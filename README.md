# Vitaline — moved into LabLedger

Vitaline's river timeline (every lab test on one shared time axis, coloured
by whether each result was in range) now lives in
[LabLedger](https://github.com/MaXiMo000/LabLedger) as the **river view** on
its Trends screen. One lab-results product, one import pipeline.

This repository is archived and kept for history. Use LabLedger.
